# Generated by reproduce-project skill.
# This file is created at the project root when none exists, to give uv a
# single declarative source of truth for Python dependencies. The actual
# package code may live in a subdir (e.g. python/, src/) and gets installed
# editable via reproduce.sh — this root pyproject.toml is dependency-only
# (no build backend, no package payload). Edit `dependencies` to match the
# real needs of the project; the skill fills it from Phase-1 discovery.

[project]
name = "__PROJECT_NAME__"
version = "0.0.0"
requires-python = ">=__PYTHON_VERSION__"
description = "reproduce-project scaffold; see scripts/reproduce.sh"
dependencies = [
    # AGENT FILLS: pull from Dockerfile / requirements / setup.py / README.
    # Example for a CUDA-heavy ML project:
    # "numpy",
    # "Cython>=0.28",
    # "onnx",
    # "protobuf",
    # "nvidia-cudnn-cu12",
]
